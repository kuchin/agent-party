# install langchain + langgraph + the Google Gemini chat model
uv add langchain langgraph langchain-google-genai

# no built-in .env loading — use python-dotenv or set directly
export GOOGLE_API_KEY="AIza..."
